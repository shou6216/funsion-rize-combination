mixin list(capsuleList)
  table.table
    thead
      tr
        th="フルネーム"
        th="略名"
    tbody
      for capsule in capsuleList
        tr
          td(scope="row"): a(href="/detail/#{capsule.id}")=capsule.fullName
          td=capsule.nickName
