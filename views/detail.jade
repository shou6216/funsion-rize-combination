include ./mixin/fusionList

extends layout
block content
  .jumbotron
    h1 #{youCapsuleName}の組み合わせ
    hr.my-2
    a.btn.btn-primary.btn-lg(href="/list" role="button") 一覧に戻る
  .row.flex-items-xs-center
    .col-md-10.col-xs-12
      - if(fusionList.length == 0) {
        組み合わせはまだありません。

      - } else {
        +list(fusionList)
      - }


